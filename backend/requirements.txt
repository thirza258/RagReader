# Pinned to the versions the test suite is green against.
#
# These were unpinned, which meant every image build silently picked up
# whatever was newest — the reason a rebuilt image could stop matching the
# code. Bump deliberately: change a pin, rebuild, run the suite.
#
# Ragas uses remote OpenRouter generation and embeddings. Install its base
# package only: the optional [all] extras pull local Transformer/Torch models.
# Hybrid reranking stays in the separate Ollama service.

# ── Django and the API ──────────────────────────────────────────────────────
Django==6.1.1
djangorestframework==3.18.0
django-cors-headers==4.9.0
drf-spectacular==0.30.0
dj-database-url==3.1.2
psycopg2-binary==2.9.12
python-dotenv==1.2.3
whitenoise==6.12.0

# ── Async and background work ───────────────────────────────────────────────
daphne==4.2.3
channels==4.3.2
channels_redis==4.3.0
celery==5.6.3
django-celery-results==2.6.0
django-redis==7.0.0
redis==8.1.0

# ── Retrieval ───────────────────────────────────────────────────────────────
openai==2.54.0                # compatible with Ragas/Instructor's jiter range
numpy==2.5.2
scikit-learn==1.9.0           # cosine similarity for dense retrieval
rank-bm25==0.2.2              # sparse retrieval
nltk==3.10.3                  # tokenization and stopwords for BM25
ollama==0.6.2                 # embeds and reranks hybrid's fused candidates

# ── Evaluation ──────────────────────────────────────────────────────────────
ragas==0.4.3
instructor==1.17.0            # Ragas structured JSON output adapter
langchain-community==0.3.31   # Ragas 0.4.3 imports the legacy VertexAI module

# ── Ingest ──────────────────────────────────────────────────────────────────
PyPDF2==3.0.1
beautifulsoup4==4.15.0
requests==2.34.2
